import FileReader from "@ant-design/icons/FireFilled"; // Adjust the icon name as per your requirement
import "../LayoutAdmin/LayoutAdmin.scss";
const Logo = () => {
  return (
    <div className="logo-siderbar">
      <div className="logo-icon">
        <FileReader />
      </div>
    </div>
  );
};

export default Logo;
